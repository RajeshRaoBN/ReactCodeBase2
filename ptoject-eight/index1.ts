type Person = {
    name: string;
}

const person: Person = {
    name: "John",
}

console.log(person.name)

// String
let myName: string = "Rajesh";
myName = "Ruben"
console.log(myName);

// Number
let index: number = 898;
console.log(index);

// Bool
let isAccepted: boolean = false;
console.log(isAccepted);